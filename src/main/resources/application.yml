datasource:
  driver: org.postgresql.Driver

server:
  port: 5000

logging:
  level:
    com:
      giraffes:
        tgbot: ${LOGGING_APP_LEVEL}

spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:db/changelog-master.xml

tg-bot:
  botUserName: ${BOT_USER_NAME}
  botToken: ${BOT_TOKEN}

purchase:
  wallet: ${WALLET_ADDRESS}
  basePrice: ${BASE_NFT_PRICE}
  presalePrice: ${PRESALE_NFT_PRICE}
  presaleQuantity: ${PRESALE_NFT_QUANTITY}
  linkToMarketplace: ${LINK_TO_MARKETPLACE}

user-wallet:
  confirmation-sum: ${CONFIRMATION_WALLET_SUM}
  confirmation-wallet: ${CONFIRMATION_WALLET}

ton-provider:
  url: ${TON_PROVIDER_URL}

p-cloud:
  api-host: ${P_CLOUD_API_HOST:eapi.pcloud.com}
  access-token: ${P_CLOUD_ACCESS_TOKEN:LSDzZ5QLjde4LwKSZiufur7Z59j9khCj2gyw3Jb7c6EzCjFFFt0V}
